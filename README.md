# EventPrimitives
Lightweight .NET/Unity primitives for state and events: EventValue (value with change event) and EventSignal (event object). Reduce boilerplate, unify event handling, and support async waiting via UniTask.
